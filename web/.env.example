# Only needed when running `pnpm dev:web` as a separate dev server against
# the API on a different port/origin. Leave unset (or empty) when the server
# serves the built web/dist itself - requests then go to the same origin.
VITE_API_URL=http://localhost:3000
